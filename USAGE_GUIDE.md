# 使用指南 / Usage Guide

## 🚀 快速开始

### 1. 环境准备

#### 1.1 创建 GitHub OAuth App

1. 访问 GitHub Settings → Developer settings → OAuth Apps
2. 点击 "New OAuth App"
3. 填写信息：
   - **Application name**: `Code Agent` (任意名称)
   - **Homepage URL**: `http://localhost:3002`
   - **Authorization callback URL**: `http://localhost:3002/github/callback` ⚠️ 必须是这个地址
4. 创建后保存 `Client ID` 和 `Client Secret`

#### 1.2 配置环境变量

创建 `.env` 文件（复制 `.env.example`）：

```bash
# GitHub OAuth 配置（必需）
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret
GITHUB_REDIRECT_URI=http://localhost:3002/github/callback

# OpenAI API 配置（可选，用于 AI 代码生成）
OPENAI_API_KEY=your_openai_api_key
OPENAI_MODEL=gpt-4o

# 数据库配置
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/code_agent
REDIS_URL=redis://localhost:6379/0

# JWT 配置
JWT_SECRET_KEY=your-secret-key-change-in-production
```

### 2. 启动服务

#### 使用 Docker Compose（推荐）

```bash
# 启动所有服务
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down
```

服务地址：
- **前端**: http://localhost:3002
- **后端 API**: http://localhost:8082
- **API 文档**: http://localhost:8082/docs

#### 本地开发模式

```bash
# 1. 启动数据库和 Redis
docker-compose up -d db redis

# 2. 启动后端
cd /workspace/137496/code_agent
source venv/bin/activate
uvicorn app.main:app --reload --host 0.0.0.0 --port 8082

# 3. 启动前端
cd frontend
npm install
npm run dev
```

---

## 📱 功能使用说明

### 1. 连接 GitHub 账户

#### 步骤：

1. **访问开发页面**
   - 打开浏览器访问 http://localhost:3002/develop

2. **点击"连接 GitHub"**
   - 在代码需求输入框下方，如果未连接 GitHub，会看到橙色提示框
   - 点击 "连接 GitHub" 按钮

3. **GitHub 授权**
   - 浏览器会跳转到 GitHub 授权页面
   - 点击 "Authorize" 授权应用访问你的仓库

4. **自动回调**
   - 授权成功后会自动跳转回应用
   - 显示 "GitHub 授权成功！" 提示
   - 1.5 秒后自动返回开发页面

5. **验证连接**
   - 顶部状态栏显示 "GitHub: 已连接"（绿色）
   - 输入框下方显示仓库和分支选择器

#### 连接状态说明：

- **未连接**: 顶部状态栏显示红色 "未连接"，输入框下方显示橙色警告
- **已连接**: 顶部状态栏显示绿色 "已连接"，可以选择仓库和分支
- **Token 存储**: Access Token 保存在浏览器 localStorage，刷新页面不会丢失

#### 断开连接：

- 点击顶部状态栏右侧的 "断开连接" 按钮
- 会清除本地 Token 和所有选择状态

---

### 2. 选择仓库和分支

#### 2.1 选择仓库

1. **查看仓库列表**
   - 连接 GitHub 后，仓库列表自动加载
   - 显示你有权限访问的所有仓库（包括个人和组织仓库）

2. **搜索仓库**
   - 在仓库选择框中输入关键词
   - 支持模糊搜索，实时过滤结果

3. **选择仓库**
   - 点击下拉列表中的仓库名称
   - 私有仓库会显示橙色 "私有" 标签
   - 选择后会自动加载该仓库的分支列表

4. **刷新仓库列表**
   - 点击仓库选择框右侧的 "刷新" 按钮
   - 会重新从 GitHub 获取最新的仓库列表

#### 2.2 选择分支

1. **查看分支列表**
   - 选择仓库后，分支列表自动加载
   - 默认会自动选中仓库的默认分支（通常是 main 或 master）

2. **搜索分支**
   - 在分支选择框中输入分支名
   - 支持模糊搜索

3. **选择分支**
   - 点击下拉列表中的分支名称
   - 受保护的分支会显示红色 "受保护" 标签

4. **刷新分支列表**
   - 点击分支选择框右侧的 "刷新" 按钮
   - 会重新从 GitHub 获取最新的分支列表

#### 2.3 仓库信息展示

选择仓库后，下方会显示仓库详细信息卡片：
- **仓库名称**: 完整的仓库名（owner/repo）
- **描述**: 仓库描述（如果有）
- **主要语言**: 显示蓝色标签
- **星标数**: ⭐ 数量
- **跳转链接**: 点击链接图标在新标签页打开 GitHub 仓库

---

### 3. AI 代码生成

#### 3.1 输入代码需求

在 "代码需求" 文本框中详细描述你的需求，例如：

```
创建一个用户登录功能，包含以下特性：
1. 邮箱和密码验证
2. 密码加密存储
3. JWT Token 生成
4. 登录失败次数限制
5. 使用 FastAPI 框架
```

#### 3.2 选择目标仓库和分支（可选）

- 如果选择了仓库和分支，AI 会考虑仓库的上下文信息
- 未选择也可以生成代码，但缺少项目上下文

#### 3.3 生成代码

1. 点击 "生成代码" 按钮
2. 等待 AI 处理（按钮显示加载动画）
3. 生成成功后，下方会显示 "生成的代码" 卡片
4. 可以点击 "复制代码" 按钮复制到剪贴板

#### 3.4 代码生成提示

**好的需求描述**：
```
✅ 详细、具体、包含技术栈
实现一个 RESTful API 用户注册接口：
- 使用 FastAPI 框架
- 接收 JSON 格式：username, email, password
- 验证邮箱格式和密码强度
- 使用 bcrypt 加密密码
- 存储到 PostgreSQL 数据库
- 返回用户 ID 和创建时间
```

**不好的需求描述**：
```
❌ 模糊、笼统、缺少细节
写一个登录功能
```

---

### 4. 配置设置

访问 http://localhost:3002/settings 配置：

#### 4.1 LLM 提供商设置

支持的提供商：
- **OpenAI**: GPT-4, GPT-3.5-turbo
- **SiliconFlow**: DeepSeek-V3
- **Qwen**: qwen-plus, qwen-turbo
- **Zhipu**: glm-4, glm-3-turbo

配置项：
- API Key（必需）
- 模型名称
- Base URL（可选）
- 最大 Token 数
- Temperature（创造性参数）

#### 4.2 GitHub 设置

- 查看当前连接状态
- 管理 OAuth 授权
- 配置 Webhook（企业版功能）

---

## 🔧 常见问题

### 1. GitHub 连接失败

**问题**: 点击 "连接 GitHub" 后跳转到错误页面

**可能原因**:
- GitHub OAuth App 配置错误
- 回调 URL 不匹配
- 环境变量未正确设置

**解决方案**:
1. 检查 GitHub OAuth App 的回调 URL 是否为 `http://localhost:3002/github/callback`
2. 确认 `.env` 中的 `GITHUB_CLIENT_ID` 和 `GITHUB_CLIENT_SECRET` 正确
3. 确认 `GITHUB_REDIRECT_URI` 为 `http://localhost:3002/github/callback`
4. 重启服务：`docker-compose restart`

### 2. 仓库列表为空

**问题**: 连接 GitHub 成功，但仓库列表为空

**可能原因**:
- Access Token 权限不足
- GitHub API 限流
- 账户没有任何仓库

**解决方案**:
1. 检查 OAuth App 的权限范围（scopes）是否包含 `repo`
2. 等待一段时间后刷新（GitHub API 有限流机制）
3. 在 GitHub 创建一个测试仓库

### 3. 分支列表加载失败

**问题**: 选择仓库后分支列表不显示

**可能原因**:
- 仓库没有任何分支
- 网络连接问题
- Token 失效

**解决方案**:
1. 点击 "刷新" 按钮重新加载
2. 检查网络连接
3. 断开并重新连接 GitHub

### 4. Token 失效处理

**问题**: 之前连接过，但现在显示未连接

**可能原因**:
- Token 过期
- Token 被撤销
- 浏览器清除了缓存

**解决方案**:
1. 点击 "连接 GitHub" 重新授权
2. 检查 GitHub Settings → Applications → Authorized OAuth Apps
3. 确认应用未被撤销授权

### 5. 代码生成失败

**问题**: 点击 "生成代码" 后显示错误

**可能原因**:
- OpenAI API Key 未配置或无效
- API 配额用尽
- 网络连接问题
- 需求描述过长

**解决方案**:
1. 检查 `.env` 中的 `OPENAI_API_KEY` 是否正确
2. 访问 OpenAI 平台检查配额
3. 简化需求描述，分步生成
4. 尝试其他 LLM 提供商（SiliconFlow、Qwen 等）

---

## 🎯 最佳实践

### 1. 安全建议

- ✅ 不要在公开仓库中提交 `.env` 文件
- ✅ 定期更换 API Keys
- ✅ 使用环境变量管理敏感信息
- ✅ 生产环境使用 HTTPS
- ✅ 设置强密码的 JWT_SECRET_KEY

### 2. 性能优化

- ✅ 使用 Redis 缓存 GitHub API 响应
- ✅ 限制代码生成的 Token 数量
- ✅ 批量操作使用异步任务
- ✅ 定期清理过期数据

### 3. 开发建议

- ✅ 详细描述代码需求，包含具体的技术栈
- ✅ 先选择仓库和分支，让 AI 了解项目上下文
- ✅ 生成代码后仔细检查，不要直接使用
- ✅ 复杂功能分步生成，逐步完善
- ✅ 使用代码审查功能验证生成的代码

---

## 📊 监控和日志

### 查看应用日志

```bash
# 查看所有服务日志
docker-compose logs -f

# 查看后端日志
docker-compose logs -f api

# 查看前端日志
docker-compose logs -f frontend

# 查看数据库日志
docker-compose logs -f db
```

### 健康检查

```bash
# 检查后端健康状态
curl http://localhost:8082/health

# 检查 Redis 连接
docker-compose exec redis redis-cli ping

# 检查数据库连接
docker-compose exec db psql -U postgres -c "SELECT 1"
```

---

## 🔄 更新和维护

### 更新代码

```bash
# 拉取最新代码
git pull origin main

# 重新构建并启动
docker-compose up -d --build

# 应用数据库迁移
docker-compose exec api alembic upgrade head
```

### 备份数据

```bash
# 备份数据库
docker-compose exec db pg_dump -U postgres code_agent > backup.sql

# 恢复数据库
cat backup.sql | docker-compose exec -T db psql -U postgres code_agent
```

---

## 📞 支持和反馈

- **GitHub Issues**: https://github.com/zxh0305/code_agent/issues
- **文档**: https://github.com/zxh0305/code_agent/wiki
- **更新日志**: [CHANGELOG.md](CHANGELOG.md)

---

## 📝 版本历史

### v0.2.0 (2024-01-22)
- ✨ 完善 GitHub OAuth 集成
- ✨ 优化 UI 布局，仓库选择器移至输入框下方
- ✨ 添加 Token 自动管理
- ✨ 新增断开连接功能
- 🐛 修复 Select 组件兼容性问题
- 🐛 修复字段映射问题（private vs is_private）

### v0.1.0 (2024-01-15)
- 🎉 初始版本发布
- ✨ GitHub OAuth 认证
- ✨ 仓库和分支管理
- ✨ AI 代码生成
- ✨ 代码分析功能
